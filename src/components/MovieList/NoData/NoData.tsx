import { FC } from 'react';

import styles from './NoData.module.css';

export const NoData: FC = () => <div className={styles.container}>Nothing found. Try another search</div>;

export default NoData;
